package main

import (
	"github.com/miyamo2/braider/pkg/annotation"
	"github.com/miyamo2/braider/pkg/annotation/app"
	"typed_inject/domain"
	"typed_inject/service"
)

var _ = annotation.App[app.Default](main) // want "bootstrap code is missing"

func main() {
	_ = dependency
}

// braider:hash:5ce43b1209481fb1
var dependency = func() struct {
	userRepository domain.IRepository
} {
	userRepository := service.NewUserRepository()
	return struct {
		userRepository domain.IRepository
	}{
		userRepository: userRepository,
	}
}()
